package server.services.login;

import server.services.base.Service;

/**
 * Logs in a user. Receives data via a LoginRequest object, parses it, and returns a LoginResponse object.
 */
public class LoginService extends Service {
    public LoginResponse login(LoginRequest request) {
        return null;
    }
}
