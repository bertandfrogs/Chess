package api.services.login;

import api.services.base.Service;

/**
 * Manages the requests and responses to and from the server for the LoginService endpoint.
 * Receives a LoginServiceRequest, parses it, and returns a LoginServiceResponse.
 * Logs in a user.
 */
public class LoginService extends Service {
    public LoginResponse login(LoginRequest request) {
        return null;
    }
}
