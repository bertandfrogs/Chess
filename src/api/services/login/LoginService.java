package api.services.login;

import api.services.base.Service;

/**
 * Manages the requests and responses to and from the server for the LoginService endpoint. Logs in a user.
 */
public class LoginService extends Service {
    /**
     * @param request A LoginRequest object to be sent to the server.
     * @return The LoginResponse from the server.
     */
    public LoginResponse login(LoginRequest request) {
        return null;
    }
}
